dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 추가 한 부분
        maven { url "https://jitpack.io" }
    }
}
rootProject.name = "Notify2CalendarLite"
include ':app'

